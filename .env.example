# 可选：根目录 .env 只用于两套档案共用 DeepSeek 设置。
# 不要把真实值粘贴到聊天或提交到 Git。
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# 微信凭证、allowlist 和滴答 OAuth 必须分别由 owner / partner 的本地向导保存，
# 不要放在这个共用文件中。启动脚本会自行设置项目配置和 Dry Run 闸门。
